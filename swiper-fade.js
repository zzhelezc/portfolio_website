var swiper = new Swiper('.swiper', {
    lazy: true,
    autoHeight: false,
    keyboard: {
        enabled: true,
    },
    loop: true,
    effect: 'fade',
    //fadeEffect: { crossFade: true },
});

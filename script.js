var swiper = new Swiper('.swiper', {
    lazy: true,
    autoHeight: false,
    keyboard: {
        enabled: true,
    },
    loop: true,
    effect: 'slide',
    // fadeEffect: { crossFade: true },
});
